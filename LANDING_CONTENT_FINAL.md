# FlatFeed Landing Content — Implemented Version

The approved landing copy is implemented in:

- `docs/case-study.html` — rendered public source;
- `CASE_STUDY.md` — synchronized readable source.

## Narrative contract

`Problem → Product → Decisions → AI Evaluation → Next → What this demonstrates`

The open linear layer keeps:

- the fragmented Berlin WBS search and saved-filter idea;
- product and evaluation status as separate lines;
- candidate ownership and coding-collaborator disclosure;
- the four-stage Telegram workflow and seven-screen product proof;
- three consequential decisions;
- the deterministic user path and separate offline AI-and-code path;
- all four aggregate synthetic results, the unusable check and the evidence
  boundary;
- the next product metrics and human-review requirement;
- an explicit hiring synthesis.

Closed details retain the field-level results, final model configuration, the
unusable-case explanation, cost assumptions, evidence links and current
prototype limits.

Do not maintain a third full copy of the landing here. Update
`docs/case-study.html` and `CASE_STUDY.md` together so the content cannot drift.
